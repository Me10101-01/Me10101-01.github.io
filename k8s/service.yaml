apiVersion: v1
kind: Service
metadata:
  name: queen-signal-ingestion
  labels:
    app: queen
    component: signal-ingestion
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: queen
    component: signal-ingestion
