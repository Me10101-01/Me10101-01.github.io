{
  "apiUrl": "${QUEEN_API_URL}",
  "timeout": 30000,
  "retries": 3,
  "healthCheck": {
    "enabled": true,
    "interval": 60000
  },
  "authentication": {
    "type": "bearer",
    "tokenEnv": "QUEEN_API_TOKEN"
  }
}
